#include <main.h>

/**
 *   main -check the code
 *
 *   Return:Always 0.
 */
int main(void)
{
	print_alphabet(a);

	alpha = 'a';

	while (alpha <= 'z')
	{
		_putchar(alpha);
		alpha++;
	}
	-putchar('\n');
}

