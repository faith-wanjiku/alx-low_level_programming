#include "main.h"

/**
 * mul-multiply two intergers 
 * @a:integer one to be multiplied
 * @b:interger two to be multiplied 
 * Return:value of the two multiplied intergers 
 */

int mul(int a, int b)
{
return(a * b);
}
